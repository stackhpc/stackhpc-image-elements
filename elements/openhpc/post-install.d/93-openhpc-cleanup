#!/bin/bash

if [ ${DIB_DEBUG_TRACE:-1} -gt 0 ]; then
    set -x
fi
set -e
set -o pipefail

if [ "$DIB_OPENHPC_DELETE_REPO" = "y" -o "$DIB_OPENHPC_DELETE_REPO" = "Y" ]
then
    rm -f /etc/yum.repos.d/OpenHPC.repo
fi

systemctl enable ntpd.service
